services:
  # frontend:
  #   image: nginx:alpine
  #   container_name: frontend
  #   restart: unless-stopped
  #   volumes:
  #     - ./frontend:/usr/share/nginx/html:ro
  #   ports:
  #     - "8080:80"
  #   depends_on:
  #     - backend
  #     # - python # uncomment me when link with python

  # backend:
  #   build: ./backend
  #   container_name: backend
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./backend:/usr/src/app

  # python:
  #   image: # put your python image here
  #   # add context if need
  #   container_name: ocr
  #   volumes:
  #     - # volume if need?
  mongo:
    image: mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_URL: mongodb://root:example@mongo:27017/
      ME_CONFIG_BASICAUTH_ENABLED: true
      ME_CONFIG_BASICAUTH_USERNAME: mongoexpressuser
      ME_CONFIG_BASICAUTH_PASSWORD: mongoexpresspass

volumes:
  pgdata: